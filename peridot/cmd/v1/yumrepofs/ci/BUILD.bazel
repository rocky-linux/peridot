load("//rules_resf:defs.bzl", "RESFDEPLOY_OUTS_MIGRATE", "container", "peridot_k8s")

container(
    base = "//bases/bazel/go",
    files = [
        "//peridot/cmd/v1/yumrepofs",
    ],
    image_name = "yumrepofs",
)

peridot_k8s(
    name = "yumrepofs",
    src = "deploy.jsonnet",
    outs = RESFDEPLOY_OUTS_MIGRATE,
    deps = ["//ci"],
)
